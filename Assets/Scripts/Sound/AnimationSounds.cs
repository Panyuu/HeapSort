using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Audio;

public class AnimationSounds : MonoBehaviour
{/*
    [SerializeField] AudioClip[] animationSound;

    void playMove1()
    {
        //Noch offen
        AudioManager.playThisNow(animationSound[0]);
    }

    void playMove2()
    {
        //Schnelle Schiffsbewegung
        AudioManager.playThisNow(animationSound[1]);
    }

    void playMove3()
    {
        //Schnelle Schiffsbewegung
        AudioManager.playThisNow(animationSound[2]);
    }

    public void PlayMove4()
    {
        //Schnelle Schiffsbewegung
        //AudioManager.playThisNow(animationSound[3]);
        animationSound[3].Play();
        Debug.Log("hey i work");
    }

    void playMove5()
    {
        //Schnelle Schiffsbewegung
        AudioManager.playThisNow(animationSound[4]);
    }

    void playCursor1()
    {
        //Textsoundding, was Anja für die einzelnen Texte wollte/Cursorsound
        AudioManager.playThisNow(animationSound[5]);
    }

    void playCursor2()
    {
        //Noch offen
        AudioManager.playThisNow(animationSound[6]);
    }

    void playCorrect1()
    {
        //Nach richtiger Auswahl des Spielers/Wenn ein Wechsel möglich ist
        AudioManager.playThisNow(animationSound[7]);
    }

    void playCorrect2()
    {
        //Nach richtiger Auswahl des Spielers/Wenn ein Wechsel möglich ist
        AudioManager.playThisNow(animationSound[8]);
    }

    void playFalse1()
    {
        //Nach falscher Auswahl des Spielers/Wenn ein Wechsel nicht möglich ist
        AudioManager.playThisNow(animationSound[9]);
    }

    void playFalse2()
    {
        //Nach falscher Auswahl des Spielers/Wenn ein Wechsel nicht möglich ist
        AudioManager.playThisNow(animationSound[10]);
    }

    void playApplause()
    {
        //Am Ende, wenn alles vollständig sortiert ist
        AudioManager.playThisNow(animationSound[11]);
    }*/
}
